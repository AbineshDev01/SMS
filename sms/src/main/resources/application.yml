spring:
  datasource:
    username: abinesh
    password: abi
    url: jdbc:mariadb://localhost:3306/sms
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  # mvc:
  #   view:
  #     suffix: .html
  # thymeleaf:
  #   prefix: /templates/

